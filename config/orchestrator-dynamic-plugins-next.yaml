plugins:
  - package: 'oci://quay.io/rhdh/red-hat-developer-hub-backstage-plugin-orchestrator:{{inherit}}'
    disabled: false
    pluginConfig:
      dynamicPlugins:
        frontend:
          red-hat-developer-hub.backstage-plugin-orchestrator:
            # Orchestrator 6.x colocates NFS at PluginRoot (no Alpha module).
            # RHDH next still serves packages/app unless APP_CONFIG_app_packageName=app-next.
            # pluginModule only preloads a scalprum module; dynamicRoutes/appIcons
            # default to PluginRoot, which does not export OrchestratorPage.
            pluginModule: Legacy
            appIcons:
              - name: orchestratorIcon
                importName: OrchestratorIcon
                module: Legacy
            dynamicRoutes:
              - path: /orchestrator
                importName: OrchestratorPage
                module: Legacy
                menuItem:
                  icon: orchestratorIcon
                  text: Orchestrator
                  textKey: menuItem.orchestrator
            menuItems:
              orchestrator:
                icon: orchestratorIcon
  - package: 'oci://quay.io/rhdh/red-hat-developer-hub-backstage-plugin-orchestrator-backend:{{inherit}}'
    disabled: false
    dependencies:
      - ref: sonataflow
  - package: 'oci://quay.io/rhdh/red-hat-developer-hub-backstage-plugin-scaffolder-backend-module-orchestrator:{{inherit}}'
    disabled: false
  - package: 'oci://quay.io/rhdh/red-hat-developer-hub-backstage-plugin-orchestrator-form-widgets:{{inherit}}'
    disabled: false
